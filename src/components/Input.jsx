import styled from "styled-components";

const Input = styled.input`
    font-size: 16px;
    font-weight: bold;
    padding: 5px;
    display: block;
    margin: 15px auto;

    :active {
        outline: none;
    }
`;

export default Input;